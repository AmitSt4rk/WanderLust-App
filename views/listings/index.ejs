<% layout("/layouts/boilerplate") %>

    <body>
        <div class="all-listing-page">
            <div class="filters">
                <%- include("./filters/filter.ejs") %>
            </div>

            <div class="listings-container">
                <div class="listing-card-containers">
                    <% if (allListings.length===0) { %>
                        <h4>No listings found.</h4>
                        <% } %>
                            <% for (const listing of allListings) { %>
                                <div class="card listing-card">
                                    <img src="<%= listing.image.url %>" class="card-img card-img-top"
                                        alt="Listing Image!">
                                    <div class="card-body">
                                        <p class="card-text">
                                            <span>
                                                &#8377; <%= Number(listing.price || 0).toLocaleString("en-IN") %> /
                                                    night
                                            </span>
                                            <span class="avg-rating">
                                                <% let ratingLength=listing.review.length; let totalRating=0; for(let
                                                    review of listing.review) { totalRating +=review.rating; } let
                                                    averageRating=ratingLength>0 ? (totalRating /
                                                    ratingLength).toFixed(1):
                                                    0;
                                                    %>

                                                    <i class="fa-solid fa-star"></i>
                                                    <%= averageRating %>
                                            </span>
                                        </p>
                                        <h5 class="card-title">
                                            <%= listing.title %>
                                        </h5>
                                        <a class="show-link" href="/listings/<%= listing._id %>">See more...</a>
                                    </div>
                                </div>
                                <% } %>
                </div>
            </div>
        </div>
    </body>